# Copy this file to .env and adjust as needed.
# Either set DATABASE_URL, or the individual PG* vars below - DATABASE_URL wins if both are set.

# DATABASE_URL=postgres://postgres:postgres@localhost:5432/user_api

PGHOST=localhost
PGPORT=5432
PGUSER=postgres
PGPASSWORD=
PGDATABASE=user_api

PORT=3000
